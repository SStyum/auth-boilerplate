services:
  postgres:
    image: postgres:16-alpine
    container_name: authbp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-authbp}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-authbp}
      POSTGRES_DB: ${POSTGRES_DB:-authbp}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-authbp} -d ${POSTGRES_DB:-authbp}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
